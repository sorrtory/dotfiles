# Software installation catalog

This is the human-readable source of truth for the selected Ubuntu user
environment: what each program is for, how it is installed, and where that
installation is defined. It covers the minimal shared toolkit and explicit
installation exceptions. Dependencies belonging only to a deferred desktop or
program configuration are added when that configuration is actually migrated.
Linked implementation files remain authoritative for exact mechanics, and
`flake.lock` remains authoritative for Nix package versions.

The host virtualization stack (QEMU/KVM, libvirt, virt-manager and UEFI firmware)
comes from distro packages through the
[`virtualization` bootstrap phase](../scripts/bootstrap/10-virtualization.sh),
supporting x86_64 Debian/Ubuntu, Fedora and Arch. The
[VM setup instructions](../README.md#virtual-machines) describe its service,
network and authorization setup.

| Software                                                   | Purpose                                                                   | Installation                                                                                      | Definition                                                                                 |
| ---------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| age                                                        | Encrypt and decrypt SOPS data                                             | Home Manager global user tool; the same store path is first fetched by the recovery app, which needs it before Home Manager exists | [Global packages](../modules/packages.nix) and [recovery app](../packages/recover-age-identity.nix) |
| Anime4K                                                    | MPV video shaders                                                         | Nixpkgs `anime4k`, symlinked into the MPV config directory as a whole store path                  | [MPV module](../modules/programs/mpv.nix)                                                  |
| AppArmor `userns` allowances | Let capture's sing-box and the Electron of Vesktop, Obsidian and VS Code use unprivileged user namespaces on Ubuntu | Exact-path profiles generated by Home Manager from executables that modules register; installed with sudo by the explicit `apparmor` phase; activation only warns when stale | [AppArmor module](../modules/apparmor.nix) and [bootstrap phase](../scripts/bootstrap/09-apparmor.sh) |
| APT repository helpers                                     | Add third-party Debian repositories                                       | Legacy-only host tooling; do not reproduce as a global user package                               | Not implemented                                                                            |
| `aria2`                                                   | Multi-connection download utility                                         | Home Manager global user tool, and the backend of `download file`; its own default of one connection per server would make it no better than wget, so the command sets `-x8 -s8` | [Global packages](../modules/packages.nix) and [Scripts module](../modules/scripts.nix) |
| Audacity                                                   | Audio editor                                                              | Deferred to desktop application review                                                            | Not implemented                                                                            |
| AyuGram (`ayugram-desktop`)                                | Telegram client, including calls                                         | Nixpkgs `ayugram-desktop` installed by the VPNized apps module with its command, desktop entry and `tg://` handler routed through the VPN command; a native Qt binary, so it needs no AppArmor allowance; login and session state remain machine-local; opened by `<Super>m` | [VPNized apps module](../modules/programs/vpnized-apps/default.nix) and [GNOME module](../modules/desktops/gnome.nix) |
| bat                                                        | Syntax-highlighting file viewer                                           | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| Bun                                                        | JavaScript runtime referenced by legacy shell configuration               | Deferred; projects own runtimes unless a global requirement is selected                           | Not implemented                                                                            |
| Chrome                                                     | Web browser                                                               | Legacy vendor-repository installer; deferred to desktop application review                        | Not implemented                                                                            |
| Claude Code (`claude`) | AI coding assistant | Home Manager global user tool from the independently pinned `sadjow/claude-code-nix` native-binary package; its launcher always selects the local sing-box HTTP proxy; authentication remains machine-local | [Global packages](../modules/packages.nix) and [flake](../flake.nix) |
| Clang (`clang++`)                                          | C and C++ compiler for builds that link host libraries                    | Host package through the `native-toolchain` bootstrap phase, with the GTK development files it compiles against; a project needing another version overrides it from a Nix development shell | [native-toolchain phase](../scripts/bootstrap/12-native-toolchain.sh)                        |
| `clang-tools` (clangd)                                     | C and C++ language server, formatter and linter                           | Home Manager global development baseline; the language server and formatters only, which collide with nothing, while the compiler above is host-owned      | [Global packages](../modules/packages.nix)                                                   |
| CMake                                                      | C and C++ build system generator                                          | Home Manager global development baseline; the generator half of the C and C++ toolchain, driving the Make already present                                   | [Global packages](../modules/packages.nix)                                                   |
| Codex (`codex`) | AI coding assistant | Home Manager global user tool from the independently pinned `sadjow/codex-cli-nix` native-binary package; its launcher always selects the local sing-box HTTP proxy; authentication remains machine-local | [Global packages](../modules/packages.nix) and [flake](../flake.nix) |
| `convert-to`                                              | Convert media files already on disk                                       | Personal script exposed in `~/.local/bin`; picks a recipe from what `ffprobe` reports, never removes anything, and refuses a batch before encoding if an output exists | [convert-to script](../scripts/bin/convert-to.sh) and [scripts module](../modules/scripts.nix) |
| curl                                                       | HTTP transfer tool                                                        | Host prerequisite for bootstrap and Home Manager global user tool                                 | [Host prerequisites](../scripts/bootstrap/01-host-deps.sh) and [global packages](../modules/packages.nix) |
| DBeaver                                                    | Database client                                                           | Legacy vendor-repository installer; deferred to desktop application review                        | Not implemented                                                                            |
| dconf Editor (`dconf-editor`)                              | Graphical dconf editor                                                    | Home Manager GNOME tool from Nixpkgs, the same on every distro                                    | [GNOME module](../modules/desktops/gnome.nix)                                              |
| delta (`git-delta`)                                        | Syntax-highlighting pager for Git diffs                                   | Nixpkgs `delta` through Home Manager's `programs.delta`, with Git integration enabled             | [Git module](../modules/programs/git.nix)                                                  |
| direnv (with `nix-direnv`)                                 | Per-directory environments, activating project development shells         | Home Manager module owning the package; `nix-direnv` caches the evaluated shell and keeps a GC root for it                                                  | [direnv module](../modules/programs/direnv.nix)                                              |
| dnsmasq | Legacy namespace DNS helper | Not selected; capture's sing-box answers DNS inside the namespace | Not implemented |
| Docker                                                     | Container runtime                                                         | Official stable convenience installer in an explicit host phase; adds the invoking user to the `docker` group; uninstall removes packages, repository setup, and all local Docker data | [Docker bootstrap phase](../scripts/bootstrap/06-docker.sh)                                 |
| `download`                                                | Fetch media through one front door                                        | Personal script exposed in `~/.local/bin`; the type argument selects yt-dlp, gallery-dl, aria2c or spotdl, and `$PROXY` is translated into each one's own spelling | [download script](../scripts/bin/download.sh) and [scripts module](../modules/scripts.nix) |
| ExifTool                                                   | Media metadata inspector                                                  | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| `fd`                                                       | Filesystem search tool                                                    | Home Manager global user tool, replacing Ubuntu's `fdfind` command name                           | [Global packages](../modules/packages.nix)                                                 |
| FFmpeg                                                     | Audio and video processing shared by scripts, `yt-dlp`, and MPV workflows | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| Firefox                                                    | Web browser                                                               | Deferred to desktop application review; login and profile state remain machine-local              | Not implemented                                                                            |
| Flatpak                                                    | Host-level desktop application infrastructure                             | Host-owned; excluded from normal Home Manager activation                                          | Not implemented                                                                            |
| fnm                                                        | Node.js version manager referenced by retained shell configuration        | Not selected; its `PATH` does not survive a GUI or session launch, which a global Nix-provided Node does not need a workaround for | Not implemented                                                                            |
| `fzf`                                                      | Interactive fuzzy finder                                                  | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| `gallery-dl`                                               | Image-gallery and booru downloader                                        | Image backend of the `download` command, from the unstable pin. Not a bootstrap phase: stable releases carry no Linux binary since v1.32.0, so there is nothing to verify and `gallery-dl -U` cannot reach the stable channel | [Global packages](../modules/packages.nix), the [download command](../scripts/bin/download.sh) and the `nixpkgs-unstable` input in [flake.nix](../flake.nix) |
| GCC and G++                                                | C and C++ compilers                                                       | Home Manager global development baseline                                                          | [Global packages](../modules/packages.nix)                                                 |
| GTK 3 development files                                    | Headers and `pkg-config` data for native Linux desktop builds             | Host package through the `native-toolchain` bootstrap phase (`gtk3-devel`, `libgtk-3-dev`, `gtk3`); Flutter's Linux desktop target links them                | [native-toolchain phase](../scripts/bootstrap/12-native-toolchain.sh)                        |
| GDB                                                        | Debugger for compiled languages                                           | Home Manager global development baseline                                                                                                                    | [Global packages](../modules/packages.nix)                                                   |
| Git                                                        | Version control                                                           | Host prerequisite for bootstrap; user package and configuration owned by a Home Manager program module| [Git module](../modules/programs/git.nix)                                                  |
| GitHub CLI (`gh`)                                          | GitHub from the terminal; signs in to clone the recovery vault            | Home Manager global user tool; the same store path is first fetched by the recovery app; its sign-in stays machine-local session state | [Global packages](../modules/packages.nix) and [recovery app](../packages/recover-age-identity.nix) |
| gitleaks                                                   | Repository secret scanner                                                 | Nix development-shell tool used by repository checks                                              | [Flake](../flake.nix)                                                                      |
| GNOME Extensions Manager (`gnome-shell-extension-manager`) | Manage GNOME Shell extensions                                             | Home Manager GNOME tool from Nixpkgs `gnome-extension-manager`, replacing the apt package; extensions installed through it by hand stay unmanaged | [GNOME module](../modules/desktops/gnome.nix)                                              |
| GNOME Shell extensions: Blur my Shell, Clipboard Indicator, Hide Top Bar, Run or Raise, User Themes | Shell appearance, clipboard history, auto-hiding top bar, application window shortcuts, and loading Rewaita's generated Shell CSS | Nixpkgs `gnomeExtensions` through Home Manager's `programs.gnome-shell`, which owns `enabled-extensions`; replaces the archived `gnome-shell-extension-installer`. Ubuntu's default extensions stay distro-provided and enabled by the session mode | [GNOME module](../modules/desktops/gnome.nix) |
| GNOME Tweaks (`gnome-tweaks`)                              | Configure additional GNOME preferences                                    | Home Manager GNOME tool from Nixpkgs, the same on every distro; its closure carries its own GNOME Shell and Mutter (about 850 MiB), accepted over a per-distro install | [GNOME module](../modules/desktops/gnome.nix)                                              |
| GnuPG                                                      | OpenPGP tooling                                                           | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| Go                                                         | Go compiler and tools                                                     | Home Manager global development baseline; replaces the downloaded system toolchain                | [Global packages](../modules/packages.nix)                                                 |
| Gradia                                                     | Screenshot annotation                                                     | Home Manager global desktop application from Nixpkgs, replacing the legacy Flatpak; `<Shift>F11` takes an interactive screenshot through the desktop portal | [Global packages](../modules/packages.nix) and [GNOME module](../modules/desktops/gnome.nix) |
| Home Manager                                               | Build and activate the user environment                                   | Flake profile activated by `04-home-manager` after secret recovery                                | [Flake](../flake.nix), [home profile](../home.nix), and [bootstrap phase](../scripts/bootstrap/04-home-manager.sh) |
| `htop`                                                     | Interactive process viewer                                                | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| HTTPie                                                     | Human-oriented HTTP client                                                | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| ImageMagick                                                | Image conversion and processing                                           | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| `iproute2`                                                 | Network namespace and interface commands                                  | Private runtime input of the VPN command's namespace-entry helper; not a host prerequisite         | [VPNized apps module](../modules/programs/vpnized-apps/default.nix)                        |
| iptables | Legacy namespace NAT helper | Not selected; namespace capture uses a TUN forwarding to the local proxy and needs no NAT | Not implemented |
| JDK 21                                                     | Java compiler and runtime                                                 | Home Manager global development baseline; replaces separate default JDK and JRE packages          | [Global packages](../modules/packages.nix)                                                 |
| jq | Command-line JSON processor, also the runtime JSON generator for the local proxy and capture | Home Manager global user tool; the same store path is also a private runtime dependency of the packaged generators | [Global packages](../modules/packages.nix), [Proxy module](../modules/programs/sing-box/default.nix) and [VPNized apps module](../modules/programs/vpnized-apps/default.nix) |
| KeePassXC                                                  | Password, recovery-code, and private age-identity storage                 | Home Manager global user package; the same store path is first fetched by the recovery app for `keepassxc-cli`; database remains outside public dotfiles | [Global packages](../modules/packages.nix), [recovery app](../packages/recover-age-identity.nix), and [bootstrap phase](../scripts/bootstrap/03-secret-recovery.sh) |
| Kitty                                                      | Terminal emulator                                                         | Deferred to desktop application and native-config review                                          | Not implemented                                                                            |
| lazygit                                                    | Terminal Git client                                                       | Not selected; nothing in the Neovim configuration ever referenced it, so no migration was waiting on it | Not implemented                                                                            |
| LibreOffice                                                | Office suite                                                              | Deferred to desktop application review                                                            | Not implemented                                                                            |
| LXD                                                        | System container manager used by the legacy proxy                         | Retired with the legacy proxy rather than migrated; see migration §13                             | Not implemented                                                                            |
| Make                                                       | Build automation                                                          | Home Manager global development baseline                                                          | [Global packages](../modules/packages.nix)                                                 |
| MesloLGS Nerd Font (`nerd-fonts.meslo-lg`)                 | Icon glyphs for Neovim's completion menu, lualine and neo-tree, and Yazi's file-type icons | Home Manager global user tool; `fonts.fontconfig.enable` is also turned on, since it defaults to off outside NixOS and the package would otherwise sit unseen by non-Nix applications; VS Code names it (as `MesloLGS Nerd Font Mono`) for its terminal, but selecting it in Ptyxis is a manual preference, not declared here | [Global packages](../modules/packages.nix)                                                 |
| MPV                                                        | Media player                                                              | Home Manager `programs.mpv` with native `mpv.conf` and `input.conf`                               | [MPV module](../modules/programs/mpv.nix)                                                  |
| MPV scripts                                                | MPV behavior extensions                                                   | Nixpkgs `mpvScripts` where they exist; only `fuzzydir` below is local, because Nixpkgs lacks it   | [MPV module](../modules/programs/mpv.nix)                                                  |
| MPV script: `thumbfast`                                    | Seek-bar thumbnail previews                                               | Nixpkgs `mpvScripts.thumbfast` with its source overridden to upstream head, one commit ahead      | [MPV module](../modules/programs/mpv.nix)                                                  |
| MPV script: `fuzzydir`                                     | Recursive `**` in MPV subtitle and audio search paths                     | Local Nix package; `mpv.conf` depends on the syntax it provides                                   | [Package](../packages/mpv-fuzzydir.nix)                                                    |
| MPV script: `uosc`                                         | On-screen controller; draws thumbfast previews                            | Nixpkgs `mpvScripts.uosc`; disables MPV's builtin OSC itself                                      | [MPV module](../modules/programs/mpv.nix)                                                  |
| Neovim                                                     | Text editor and default `$EDITOR`                                         | Home Manager program package; the native Lua tree in `configs/nvim` is one live-editable symlink, and `lazy.nvim` with mason keeps the plugins | [Neovim module](../modules/programs/neovim.nix)                                            |
| NetworkManager applet                                      | Desktop interface for the host-owned network manager                      | Host-integrated desktop software; deferred to desktop migration                                   | Not implemented                                                                            |
| Nix                                                        | User-environment package and build system                                 | Multi-user installation through bootstrap                                                         | [Nix bootstrap phase](../scripts/bootstrap/02-nix.sh)                                      |
| Ninja                                                      | Build backend for generated build systems                                 | Home Manager global development baseline; the generator CMake presets usually name                                                                          | [Global packages](../modules/packages.nix)                                                   |
| Node.js                                                    | JavaScript runtime                                                        | Home Manager global user tool; selected globally because mason installs ten of Neovim's language servers as npm packages that need Node at runtime | [Global packages](../modules/packages.nix)                                                 |
| OBS Studio (`obs-studio`)                                  | Recording and streaming                                                   | Home Manager global desktop application                                                           | [Global packages](../modules/packages.nix)                                                 |
| Obsidian                                                   | Knowledge-base application                                                | Home Manager program package started with `--proxy-server` for the local HTTP proxy while it is enabled; needs the `apparmor` phase on Ubuntu; vaults and session state remain machine-local | [Obsidian module](../modules/programs/obsidian.nix)                                        |
| Oh My Zsh and Zsh plugins                                  | Interactive shell framework and extensions                                | Home Manager packages; selected built-ins plus packaged autosuggestions and syntax highlighting   | [Zsh module](../modules/programs/zsh.nix)                                                  |
| OpenSSH client                                             | SSH access used by Git and remote workflows                               | Host-owned prerequisite; private keys become reproducible SOPS secrets and `~/.ssh` configuration becomes repository material in the SSH milestone | Not implemented                                                                            |
| pipx                                                       | Isolated Python application installer                                     | Legacy dependency of Python `tldr`; replaced for that use and otherwise deferred                  | Not implemented                                                                            |
| `pkg-config`                                               | Native build dependency discovery                                         | Host package through the `native-toolchain` bootstrap phase; the Nixpkgs binary searches only its own store path, so on `PATH` it hid the host's `.pc` files, and Nix development shells bring their own | [native-toolchain phase](../scripts/bootstrap/12-native-toolchain.sh)                        |
| pnpm                                                       | JavaScript package manager                                                | Deferred; projects own package managers unless a global requirement is selected                   | Not implemented                                                                            |
| Powerlevel10k                                              | Zsh prompt theme                                                          | Retired; the legacy shell selected `flazz` and had no Powerlevel10k config or theme checkout       | Not implemented                                                                            |
| Rewaita                                                    | Recolor GTK, GNOME Shell and Firefox while retaining Adwaita's structure  | Home Manager GNOME package pinned to upstream 1.1.7 because Nixpkgs 1.1.1 lacks the guide's CLI and Firefox support; Gruvbox Medium with GNOME's orange accent is reapplied at graphical login, while generated CSS and Fine Tune preferences remain mutable | [Local package](../packages/rewaita.nix) and [GNOME module](../modules/desktops/gnome.nix) |
| Python                                                     | Python runtime and development environment                                | Not selected as a global baseline; projects own required versions                                 | Not implemented                                                                            |
| ripgrep                                                    | Recursive text search                                                     | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| Rust and Cargo                                             | Rust compiler and package manager                                         | Home Manager global development baseline; replaces Rustup for the global default                  | [Global packages](../modules/packages.nix)                                                 |
| screen                                                     | Terminal multiplexer                                                      | Legacy candidate expected to be removed in favor of tmux                                          | Not implemented                                                                            |
| Shadowsocks                                                | Proxy used by the legacy LXD setup                                        | Retired with the legacy LXD proxy rather than migrated; see migration §13                         | Not implemented                                                                            |
| ShellCheck (`shellcheck`)                                  | Shell script linter                                                       | Home Manager global user tool; bash-language-server in Neovim reports its diagnostics only when it is on PATH | [Global packages](../modules/packages.nix)                                                 |
| sing-box | Per-machine WireGuard local proxy, and the capture namespace behind the VPN command | Home Manager user services from a `nixos-unstable` input for 1.14 namespace support: the backend from per-device SOPS ciphertext, and a credential-free capture started on demand; no host routing changes | [Proxy module](../modules/programs/sing-box/default.nix) and [VPNized apps module](../modules/programs/vpnized-apps/default.nix) |
| Snap                                                       | Host-level desktop and container application infrastructure               | Host-owned; excluded from normal Home Manager activation                                          | Not implemented                                                                            |
| sops                                                       | Encrypted configuration editor                                            | Home Manager global user tool; also in the development shell, where the secret gate asks it whether a staged file is encrypted | [Global packages](../modules/packages.nix)                                                 |
| `spotdl`                                                  | Download Spotify tracks with real metadata                                | Backend of `download` for Spotify URLs and the `saved` query, from the unstable pin; a YouTube URL reaches it only with `--spotify-meta` | [Scripts module](../modules/scripts.nix) and the `nixpkgs-unstable` input in [flake.nix](../flake.nix) |
| Spotify                                                    | Music application                                                         | Home Manager global desktop application                                                           | [Global packages](../modules/packages.nix)                                                 |
| Sublime Merge                                              | Git client                                                                | Legacy vendor-repository installer; deferred to desktop application review                        | Not implemented                                                                            |
| Sublime Text                                               | Text editor                                                               | Home Manager program package linking the OpenSSL 1.1 its own tarball ships, until Nixpkgs' stable `sublime4` reaches build 4205, whose plugin host links OpenSSL 3; then the override is removed. Evaluation only without a license          | [Sublime Text module](../modules/programs/sublime-text.nix)                                 |
| tealdeer                                                   | `tldr` documentation client                                               | Home Manager module owning the package, replacing the pipx-installed Python client; native `config.toml` in `configs/tealdeer` is a live-editable symlink | [tealdeer module](../modules/programs/tealdeer.nix)                                        |
| tmux                                                       | Terminal multiplexer                                                      | Home Manager module owning the package; native configuration in `configs/tmux` is a live-editable symlink | [tmux module](../modules/programs/tmux.nix)                                                |
| tmux plugins: `sensible`, `resurrect`, `continuum`         | Sensible defaults, and saving and restoring sessions                      | Nixpkgs `tmuxPlugins`, linked into the tmux config directory under their upstream names; TPM is retired, so no fetch and no `prefix + I` | [tmux module](../modules/programs/tmux.nix)                                                |
| tmuxinator                                                 | tmux project launcher referenced by shell configuration                   | Not selected; the legacy custom launcher helpers were retired as unnecessary                      | Not implemented                                                                            |
| Transmission                                               | BitTorrent client and command-line tooling                                | Deferred to desktop application and retained-script review                                        | Not implemented                                                                            |
| `tree`                                                     | Directory tree viewer                                                     | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| `util-linux` namespace tools                               | `nsenter`, `unshare`, `setpriv` and `mount` used to enter the capture namespace | Private runtime inputs of the VPN command's namespace-entry helper; not host prerequisites   | [VPNized apps module](../modules/programs/vpnized-apps/default.nix)                        |
| uv | Python project, environment and tool manager | Home Manager global user tool; projects select their own Python versions and dependencies | [Global packages](../modules/packages.nix) |
| Vesktop                                                    | Discord client, including voice                                           | Nixpkgs `vesktop` installed by the VPNized apps module with its command, desktop entry and `discord://` handler routed through the VPN command; settings in `configs/vesktop` are live-editable; login state stays machine-local; opened by `<Super>d` | [VPNized apps module](../modules/programs/vpnized-apps/default.nix) and [GNOME module](../modules/desktops/gnome.nix) |
| Vim                                                        | Text editor                                                               | Legacy candidate expected to be replaced by Neovim; the package stays undeclared, while `configs/vim/.vimrc` ships as repository material for remote servers | Not implemented as a package; `.vimrc` delivery in the [Neovim module](../modules/programs/neovim.nix) |
| Visual Studio Code                                         | Code editor                                                               | Home Manager program package; `settings.json` and `keybindings.json` in `configs/vscode` are live-editable symlinks that VS Code and Settings Sync write through; Settings Sync installs extensions, and `extensions.list` is their inventory; needs the `apparmor` phase on Ubuntu | [VS Code module](../modules/programs/vscode.nix)                                           |
| wget                                                       | HTTP and file download tool                                               | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| WireGuard tools (`wireguard-tools`)                        | WireGuard interface commands                                              | Home Manager global user tool; the package wraps `wg-quick` with its own dependencies, so `sudo` needs its absolute path rather than a bare name | [Global packages](../modules/packages.nix)                                                 |
| `wl-clipboard`                                             | Wayland clipboard commands                                                | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| `xclip`                                                    | X11 clipboard command                                                     | Home Manager global user tool; the X11 counterpart of `wl-clipboard`, selected because both the tmux copy chain and Yazi's clipboard plugin choose their tool by session type | [Global packages](../modules/packages.nix)                                                 |
| xdg-utils                                                  | Desktop opener used by the retired navigation helper                      | Deferred until a selected desktop application or script requires it                               | Not implemented                                                                            |
| `xsel`                                                     | Alternative X11 clipboard command                                         | Not selected; it is only the third branch of the tmux copy chain, which falls through to `xclip` before it and needs nothing when absent | Not implemented                                                                            |
| Yazi                                                       | Terminal file manager                                                     | Home Manager program package; native TOML configuration in `configs/yazi` is a live-editable symlink; no Snap | [Yazi module](../modules/programs/yazi.nix)                                                |
| Yazi plugin: `clipboard`                                   | Put the selected files themselves on the system clipboard                 | Nixpkgs `yaziPlugins.clipboard`; writes `text/uri-list` through `wl-copy` or `xclip`, so a GUI application pastes a file rather than a path | [Yazi module](../modules/programs/yazi.nix)                                                |
| Yazi plugin: `copy-file-contents`                          | Copy what is inside the selected files, as text                           | Local Nix package at the revision `configs/yazi/package.toml` pins; Nixpkgs does not carry this plugin | [Package](../packages/yazi-copy-file-contents.nix)                                         |
| yq                                                         | Command-line YAML, TOML and XML processor, `jq`'s syntax against those formats | Home Manager global user tool                                                                     | [Global packages](../modules/packages.nix)                                                 |
| `yt-dlp`                                                   | Media downloader                                                          | Verified official standalone Linux release in `~/.local/bin`; updates remain explicit through `yt-dlp -U`; it is the audio and video backend of the `download` command, which resolves it from `PATH` and falls back to the path this phase owns | [yt-dlp bootstrap phase](../scripts/bootstrap/05-yt-dlp.sh) and the [download command](../scripts/bin/download.sh) |
| zoxide                                                     | Directory-jumping tool                                                     | Home Manager program with declarative Zsh integration                                              | [Zsh module](../modules/programs/zsh.nix)                                                  |
| Zsh                                                        | Interactive shell                                                         | Host package for a stable login path; Home Manager owns the interactive program and generated startup files | [Host prerequisites](../scripts/bootstrap/01-host-deps.sh), [Zsh module](../modules/programs/zsh.nix), and [login-shell phase](../scripts/bootstrap/07-login-shell.sh) |
