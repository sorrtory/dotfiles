#!/usr/bin/env bash

set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
readonly REPO_ROOT

"$REPO_ROOT/scripts/repo/check-bootstrap-phases.sh" --staged
exec "$REPO_ROOT/scripts/repo/check-secrets.sh" --staged
