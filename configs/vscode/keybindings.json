// Place your key bindings in this file to override the defaults
[
    // ── Toggle Minimap
    {
        "key": "ctrl+m",
        "command": "editor.action.toggleMinimap",
    },
    // ── Toggle Maximized Panel
    {
        "key": "ctrl+[Quote]",
        "command": "workbench.action.toggleMaximizedPanel",
        "when": "panelAlignment == 'center' || panelPosition != 'bottom' && panelPosition != 'top'",
    },
    /// ── Multi-cursor line manipulation
    {
        "key": "alt+down",
        "command": "editor.action.insertCursorBelow",
        "when": "editorTextFocus",
    },
    {
        "key": "alt+up",
        "command": "editor.action.insertCursorAbove",
        "when": "editorTextFocus",
    },
    /// ── Move line up/down
    {
        "key": "ctrl+shift+down",
        "command": "editor.action.moveLinesDownAction",
        "when": "editorTextFocus && !editorReadonly",
    },
    {
        "key": "ctrl+shift+up",
        "command": "editor.action.moveLinesUpAction",
        "when": "editorTextFocus && !editorReadonly",
    },
    // ── Disable ctrl+shift+c to open system terminal
    {
        "key": "ctrl+shift+c",
        "command": "-workbench.action.terminal.openNativeConsole",
        "when": "!terminalFocus",
    },
    /// ── Disable ctrl+shift+w to close vs code window
    {
        "key": "ctrl+shift+w",
        "command": "-workbench.action.closeWindow",
    },

    // ── Focus Editor
    {
        "key": "ctrl+shift+w e",
        "command": "workbench.action.focusActiveEditorGroup",
    },

    // ── Focus Terminal
    {
        "key": "ctrl+shift+w t",
        "command": "workbench.action.terminal.focus",
    },

    // ── Focus Explorer / File Manager
    {
        "key": "ctrl+shift+w f",
        "command": "workbench.view.explorer",
    },
    {
        "key": "ctrl+shift+w f",
        "command": "workbench.files.action.focusFilesExplorer",
        "when": "explorerViewletVisible",
    },

    // ── Focus Copilot (side panel)
    {
        "key": "ctrl+shift+w c",
        "command": "workbench.action.chat.open",
    },

    /// Moving cursor with selection
    {
        "key": "alt+right",
        "command": "editor.action.moveCarretRightAction",
        "when": "editorFocus && (editorHasSelection || editorHasMultipleSelections ",
    },
    {
        "key": "alt+left",
        "command": "editor.action.moveCarretLeftAction",
        "when": "editorFocus && (editorHasSelection || editorHasMultipleSelections ",
    },

    /// SQL tools: Execute current query
    {
        "key": "ctrl+enter",
        "command": "sqltools.executeCurrentQuery",
        "when": "editorTextFocus && editorLangId == 'sql'",
    },
    // New file
    {
        "key": "ctrl+n",
        "command": "explorer.newFile",
    },
    // New folder
    {
        "key": "alt+n",
        "command": "workbench.files.action.createFolderFromExplorer",
    },
    // Toggle markdown preview
    {
        "key": "ctrl+k ctrl+v",
        "command": "markdown.togglePreview",
        "when": "!notebookEditorFocused && resourceLangId =~ /^(markdown|prompt|instructions|chatagent|skill)$/"
    },
    {
        "key": "ctrl+shift+v",
        "command": "-markdown.togglePreview",
        "when": "!notebookEditorFocused && resourceLangId =~ /^(markdown|prompt|instructions|chatagent|skill)$/"
    },
    // Reve
    {
    "key": "alt+f",
    "command": "workbench.files.action.showActiveFileInExplorer"
    }

]